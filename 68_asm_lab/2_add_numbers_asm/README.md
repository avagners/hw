# Addition Calculator in x86-64 Assembly

Программа на ассемблере x86-64 для сложения двух чисел с вводом от пользователя.

## 📁 Структура проекта

```bash
2_add_numbers_asm/
├── src/
│ └── add_numbers_input.asm # Исходный код
└── README.md # Этот файл
```

## 🛠️ Требования

- **NASM** (Netwide Assembler)
- **LD** (линкер)
- **Linux** x86-64

### Установка на Ubuntu/Debian:
```bash
sudo apt update
sudo apt install nasm binutils
```

## Компиляция и запуск

```bash
# Переход в директорию с исходным кодом
cd 2_add_numbers_asm/src

# Компиляция
nasm -f elf64 add_numbers_input.asm -o add_numbers_input.o

# Линковка
ld add_numbers_input.o -o add_numbers_input

# Запуск
./add_numbers_input
```

## Пример работы программы
```bash
Enter first number: 15
Enter second number: 7
The result is: 22
```

## 🔍 Объяснение кода
Структура программы:
Вывод приглашений для ввода чисел  
Чтение ввода от пользователя  
Преобразование строк в числа (atoi)  
Сложение чисел  
Преобразование результата в строку (itoa)  
Вывод результата  

## Особенности реализации
Системные вызовы:
write (rax=1) - вывод текста
read (rax=0) - чтение ввода
exit (rax=60) - завершение программы

Алгоритм преобразования:
atoi: Посимвольное чтение, умножение на 10, добавление цифры
itoa: Деление на 10, преобразование остатков в символы

Регистры:
r8, r9 - хранение чисел для сложения
rax - аккумулятор, возврат значений
rdi, rsi, rdx - аргументы системных вызовов

## ⚠️ Ограничения и особенности
Ограничения ввода:
- 📝 Только числа - буквы вызовут некорректные результаты
- 🔢 Максимальная длина - 16 символов на число
- 💾 Размер чисел - ограничен 64-битными целыми

## Особенности работы:
- ✅ Автоматический расчет длин строк через equ $ - label
- ✅ Буферы в секции .bss - экономия места в бинарнике
- ✅ Чистый ассемблер - без зависимостей от libc

## 📖 Полезные команды
```bash
# Просмотр секций:
readelf -S add_numbers_input

# Размер исполняемого файла:
size add_numbers_input
ls -lh add_numbers_input

# Дизассемблирование:
objdump -d add_numbers_input | less
```