# Hello World in x86-64 Assembly

Простая программа "Hello World" на ассемблере x86-64 для Linux.

## 📁 Структура проекта

1_hello_asm/
├── src/
│ └── hello.asm # Исходный код
└── README.md # Этот файл

## 🛠️ Требования

- **NASM** (Netwide Assembler)
- **LD** (линкер)
- **Linux** x86-64

### Установка на Ubuntu/Debian:
```bash
sudo apt update
sudo apt install nasm binutils
```

## 🚀 Компиляция и запуск
```bash
# Переход в директорию с исходным кодом
cd 1_hello_asm/src

# Компиляция
nasm -f elf64 hello.asm -o hello.o

# Линковка
ld hello.o -o hello

# Запуск
./hello
```

## Ожидаемый вывод
```bash
Hello, World!
```

## 📖 Полезные команды
```bash 
# Просмотр секций бинарного файла:
readelf -S hello

# Дизассемблирование:
objdump -d hello
```

## 🎯 Особенности этого кода
✅ Чистый ассемблер - без зависимостей от libc
✅ Прямые системные вызовы - использует syscall
✅ Минимальный размер - только необходимый код
✅ x86-64 специфичный - использует 64-битные регистры

Примечание: Этот код работает только на Linux x86-64 системах.